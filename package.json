{
  "name": "navigation",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "serve": "rm -rf .cache dist && parcel src/index.html",
    "build": "rm -rf .cache dist && parcel build src/index.html --no-minify --public-url ./"
  }
}
